# MyPackage

## Introduction
This package provides simple tools for analyzing and visualizing numeric data.

## Installation
To install locally:
```bash
pip install -e .

from mypackage import DataAnalyzer, DataVisualizer
data = [1,2,3,4]
analyzer = DataAnalyzer(data)
print(analyzer.mean())

MIT
